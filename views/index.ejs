<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css?family=Quicksand&display=swap" rel="stylesheet">
    <style>
        body {
            
            font-family: 'Quicksand', sans-serif;
            background-color: #f5f5f5;
            color: #434343;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 0;
            margin: 0;
        }
        .formulario{
            width: 100%;
            margin: 0;
            padding: 1rem 2rem;
            gap: 0.25rem;
            background: #004c99;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            color: #fff;
        }
        .formulario h1 {
            font-size: 1.5rem;
        }
        .formulario button, .formulario input{

            border-radius: 20px;
            border: none;
            margin: 0.25rem;
            font-size: .75rem;
            padding: 0.7rem;
        }
        .formulario input{
            background-color: #fff;
            width: 6rem;
            text-align: center;
            color: #434343;
        }
        .formulario button{
            background: #434343;
            color: #fff;
            font-weight: 600;


        }
        
    </style>
    <title>Consulta veículos</title>
</head>
<body>
    <form action="/" method="get" class="formulario">
    <h1>Consulta de placas</h1>
        <div class="form">
        <label for="placa">Digite a placa </label>
        <input type="text" id="placa" name="placa" required value="<%= placa || '' %>" maxlength=8 pattern="^[a-zA-Z]{3}.*" placeholder="Ex: XXX 0000">
        <button type="submit">Buscar</button>
        </div>
    </form>
    <% if(placa) { %>
        <hr>
        <div class="resultado">
        <h2>Resultado: </h2>
        <% if (carro) { %>
            <p><strong>Veiculo: </strong><%= carro.Veiculo %></p>
            <p><strong>Placa: </strong><%= carro.Placa %></p>
            <p><strong>Nome: </strong><%= carro.Nome %></p>
            <p><strong>Setor: </strong><%= carro.Setor %></p>
        <% } else { %>
            <p>Placa "<%= placa %>" nao encontrado</p>
        <% } %>
    <% } %>
        </div>
</body>
</html>